"""One module per agent.

Each module names, in its docstring and in `AGENT_CLASS`, which of the
organisers eight agent classes it belongs to (docs/ARCHITECTURE.md 4). Spark
occupies seven of the eight, and `tests/test_schemas.py` checks that claim
against the code rather than against the slide.
"""
